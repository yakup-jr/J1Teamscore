1. Оправдано ли использование в некоторых классах valueOf с 
параметрами вместо public конструктора, где нужна какая-то валидация? 
Видел такую реализацию в стандартных библиотеках, например, класс Integer
Т.е. насколько я понимаю использование valueOf оправдано, 
если нужна какая-то логика помимо стандартного присваивания
2. Если класс не подразумевает инстанцирование (например, RandomGenerator), 
хорошей ли практикой является "прятать" default конструктор делая его private?
3. в тесте PrioritizeStackTest assertEquals вызывает equals у Object, что сравнивает объекты по ссылке,
из-за чего по сути равные Elements возвращали false. Поэтому я решил вынести их в начало метода, чтобы ссылки были равны.
Насколько подходит данное решение? Есть ли какие-то альтернативы?